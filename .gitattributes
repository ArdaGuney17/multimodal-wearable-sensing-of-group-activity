# Raw sensor data is archived on Zenodo (see docs/data_provenance.md), NOT committed to this
# repo at all — not even via LFS. `python -m src.data.download` pulls it into data/raw/.
# This repo should not need Git LFS for anything; keeping the file in case a future large
# reference asset genuinely needs it.
#
# results/tables/*.csv are small, human-diffable reproduction outputs — deliberately plain
# git, so they render/diff normally on GitHub.
