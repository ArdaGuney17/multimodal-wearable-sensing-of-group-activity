# Thesis table → pipeline stage → original Drive source

This is the working map between what the thesis reports (`thesis_reproduction_targets.md`)
and where the corresponding logic currently lives in the messy Drive/Colab history
(`drive_source_inventory.md`). It exists so that porting a notebook into `src/` is a
lookup, not a re-investigation, and so gaps are visible instead of silently skipped.

Status legend: ⬜ not started · 🟨 in progress · ✅ ported + numbers verified against thesis · ⚠️ ported but numbers don't match / needs review

| Stage | Thesis target(s) | Best Drive source | Repo destination | Status |
|---|---|---|---|---|
| Raw sensor sync & cleaning | Ch.4 (Tables 4.1–4.4) | **Found 2026-09-04** (previously ⬜, code believed lost): `FINAL_ARDA_THESIS.ipynb` (38.6MB, earliest-vintage notebook in this repo — openearable + xsens, all 9 groups: raw-ELAN reading, real-name anonymization to `Participant1/2/3`, sensor-grid merge, and a peak-alignment sync against an ELAN `synchronizaiton_move` anchor label, heavily hand-tuned per group) and `OPTI_TRACK_PROCESSING.ipynb` (56.5MB — optitrack, all 9 groups: Hungarian-algorithm + hand-curated tracklet-stitching marker reconstruction, then an analogous movement-score-peak sync against the same ELAN anchor). Full traces in `notebooks_reference/FINAL_ARDA_THESIS_ANALYSIS.md` / `notebooks_reference/OPTI_TRACK_PROCESSING_ANALYSIS.md`. Superseded/superset of `sensor_sync_fixed.ipynb` (group-1-only) and `Arda_Thesis_Data_Preprocessing_FIXED_v4.ipynb` (groups 1,2,3,5,6 only) — both of those covered a subset of what these two notebooks cover completely. | `src/preprocessing/raw_sync_oe_xsens.py`, `src/preprocessing/raw_sync_optitrack.py` | 🟩 **`raw_sync_oe_xsens.py`: fixed + real-data-validated for Group 1 (OE+Xsens), 2026-09-04.** A background agent this session found the previous port assumed a nonexistent `"time_s"` column on the raw OpenEarable/Xsens files; a second, interrupted agent left no partial fix (git diff was empty at session start). Root cause fully diagnosed by direct inspection of `FINAL_ARDA_THESIS.ipynb`'s real cells (not just the analysis doc): raw OE per-stream files are headerless, column 0 is `timestamp_us` (absolute epoch microseconds), verbatim `SCHEMA`/`load_sensor` re-ported as `OE_SCHEMA`/`load_sensor_stream` (also fixing a second bug: the `"mgnt"` stream's value columns are `mag_x/y/z`, not `mgnt_x/y/z` — confirmed by grepping the notebook's own `SCHEMA` dict). Xsens raw files use `SampleTimeFine` (device-relative ticks), re-ported in `load_xsens_participant`/`merge_xsens_group` verbatim from CELL 5. The actually-missing step — video-relative `video_time_s`, without which ELAN labels (video-relative `begin_s`/`end_s`) were being attached against raw device/epoch time — is now computed via new `add_oe_video_time`/`add_xsens_video_time`, using new `GROUP_SYNC_CONFIG[g].video_start_us` (OE, absolute epoch) / `.xsens_to_video_offset_s` (Xsens, additive) fields; **all 9 groups' constants grepped verbatim from the notebook's own literal `VIDEO_START_US =`/`XSENS_TO_VIDEO_OFFSET_S =` cells** (not just transcribed from a task brief), with full cell-level re-verification (not just grep) for groups 1, 3, and 7 (one literal-int group, one computed-`pd.Timestamp` group, one mid-sequence group with distinct search windows per sensor). Confirmed empirically (see below) that the existing peak-search `GROUP_SYNC_CONFIG`/`compute_peak_shift` machinery is a genuine SECOND, ADDITIONAL correction layer on top of `video_time_s` — not a replacement for it — matching notebook CELL 79 (Group 7), which computes its shift purely from an already-video-aligned `video_time_s`, identically for OE and Xsens, with no extra device-offset term; the previous port's `device_start_offset_s` field (an ad-hoc "+correction inside compute_peak_shift", really just Group 7's `xsens_to_video_offset_s`=59.0 applied a second time) was therefore redundant and removed. Also fixed: `ACC_PREFIX_TEMPLATE`/`ACC_AXIS_SUFFIXES` assumed xsens kept mixed-case `p{n}_Acc_X` columns, which never matched `merge_xsens_group`'s actual (lower-cased, per CELL 5) output — this silently broke `compute_peak_shift`'s xsens signal lookup; `attach_tier_label`'s label-validity check fixed from `<=` to strict `<` on `end_s` to match the notebook's merge_asof-based labeling cells; and the shift-application step itself was rewritten from "shift the original ELAN times and re-run label attachment" to `shift_label_column`/`shift_labeled_frame` (re-derive each label segment's boundaries from the already-labeled grid, shift, and re-paint via range mask) after empirically finding the former does not reproduce the real-data fixture — the latter is the notebook's own actual algorithm (verbatim from CELL 17 and CELL 79's own `shift_label_column`/`shift_all_labels`, including a real, confirmed inconsistency between the two cells' end-bound inclusivity, preserved via a new `shift_end_inclusive` config field). **Validation** (`data/external/thesis_data/RAW_VALIDATION/run_group1_validation.py`, run against the real, already-downloaded Group 1 fixtures): OpenEarable unshifted output (`group_1_openearable_labeled.csv`, Group 1 has `apply_shift_openearable=False`) is an **exact match** — 99,765/99,765 rows, all 67 columns, 0 mismatches. Xsens last10pct-peak-shifted output (`group_1_xsens_labeled_shifted_by_last10_peak.csv`, reconstructed via `compute_peak_shift`+`shift_labeled_frame` since Group 1 also has `apply_shift_xsens=False` so `run_all()` itself wouldn't write this variant) matches exactly on row count, `SampleTimeFine`, the reconstructed time axis, and all 27 sensor-value columns (0/60,389 mismatches), and on 6 of 7 label tier columns exactly; the 7th (`label_Whole_Group`) plus 2 others each have exactly 1 mismatched row (3 total mismatched cells out of 60,389×7=422,723 label cells, ~0.0007%) — root-caused precisely to the fixture's own source cell (notebook CELL 17) using a different, inconsistent (inclusive-both-ends) labeling function than every other labeling cell in the notebook for its *unshifted* intermediate frame; not chased further since this specific file was itself never adopted into the thesis's real selected outputs (`apply_shift_xsens=False`). Existing smoke test claimed by a previous session's status update (`smoke_raw_sync_oe_xsens.py`) **does not actually exist in the repo** (`git`/`find` confirm no such file under `src/` or `tests/`) — flagged as a stale doc claim, not fixed as part of this task (out of the stated fix scope); ad hoc synthetic sanity checks this session (all 6 `search_mode` branches, `occurrence` first/last, `shift_end_inclusive` True/False, artifact cleaning, graceful `run_all()` error handling) all passed. Groups 2, 3, 5, 6, 8, 9, 10 use the same corrected machinery but are **not yet validated against real data** (no fixtures downloaded for them this session) — only Group 1 has been. `raw_sync_optitrack.py` (untouched this session, out of scope): reads the already-cleaned `group_{g}_optitrack_cleaned_combined_240hz.csv` (raw marker reconstruction/tracklet-stitching explicitly out of scope, per the note below), computes each group's single-point or two-point-linear-drift shift from its hardcoded human-confirmed constant, and transfers ELAN labels onto every row — all 9 groups; still 🟨 ported + smoke-tested on synthetic data only (`smoke_raw_sync_optitrack.py`'s existence was not re-checked this session). Both modules preserve the real, reusable candidate-detection algorithms (peak-search / `auto_detect_optitrack_sync`) alongside the literal constants actually used, so a future run can re-verify or re-derive a shift rather than only replay the recorded one. OptiTrack's raw marker-identity reconstruction — **Group 1: closed, 2026-09-10/11.** Traced cell-by-cell (not assumed) through every Group-1 OptiTrack cell in `OPTI_TRACK_PROCESSING.ipynb`: the notebook's three `scipy.optimize.linear_sum_assignment` Hungarian-tracker variants ("FAST FIRST PASS"/"CONSERVATIVE"/"BALANCED") are all abandoned dead ends — none of their output dirs is ever read downstream. The real authoritative path is a hand-curated marker-name allowlist (`GROUP1_CHAINS`), traced via the real `IN_DIR`/`OUT_DIR` chain through "MANUAL TRACKLET STITCHING" → "UPDATED MANUAL TRACKLET STITCHING" → "COMBINE TAKE 1 + TAKE 2" (whose own `out_path` literally is `group_1_optitrack_cleaned_combined_240hz.csv`). Ported as `reconstruct_markers_group1()` in `raw_sync_optitrack.py`; validated against the real raw Motive take exports (`Arda_Thesis_Group-1_Take_{1,2}.csv`) and the pre-existing known-good fixture: **474,995 rows, all 20 columns, 0 mismatches — bit-for-bit exact.** See `docs/session_2026-09-05_autonomous_progress.md`'s 2026-09-10/11 entry for the full trace. **Groups 2, 3, 5, 7, 8, 9, 10: closed, 2026-09-11 — same rigor applied, each group's OWN cells traced individually (not assumed transferable from Group 1's), and the shape genuinely differs per group** (Groups 2/3/5: single stitching cell, no `optitrack_quality_note` column in the real output; Groups 7/8: stitching cell + a diagnostic-only "gap candidate inspection" cell that is NOT read by the combine cell, `optitrack_quality_note` present; Groups 9/10: a genuine "MANUAL TRACKLET STITCHING UPDATED" cell that overwrites and supersedes the first-pass chains, `optitrack_quality_note` present; Group 10 additionally has a byte-identical duplicate combine cell, diffed directly and confirmed harmless). All 7 share the same per-take stitch/smooth helpers and the same combine mechanism (literal hardcoded per-take capture-start offset, not derived), so ported as one shared `reconstruct_markers_multi_take()` plus per-group `GROUP{N}_CHAINS`/`GROUP{N}_TAKE_OFFSETS_S`/`GROUP{N}_QUALITY_NOTES` config and thin `reconstruct_markers_group{N}()` wrappers in `raw_sync_optitrack.py`. Raw Motive take files for all 7 groups were already present locally (no downloads needed). Validated against each group's real raw takes and its already-known-good `group_N_optitrack_cleaned_combined_240hz.csv`, **every group bit-for-bit exact, 0 mismatches**: Group 2 — 645,503 rows × 20 cols; Group 3 — 643,885 × 20; Group 5 — 820,200 × 20; Group 7 — 731,083 × 21; Group 8 — 668,173 × 21; Group 9 — 880,464 × 21; Group 10 — 359,887 × 21. See `docs/session_2026-09-05_autonomous_progress.md`'s 2026-09-11 "OptiTrack raw marker reconstruction, Groups 2/3/5/7/8/9/10" entry for the full per-group trace. **8 of 9 study groups' OptiTrack raw-marker reconstruction is closed** (Groups 1,2,3,5,7,8,9,10 — Group 4 excluded everywhere for camera failure). **CORRECTION (2026-09-11): the claim here and in earlier commits that "Group 6 has no OptiTrack recording" was never independently verified and is false.** `GROUP_SYNC_CONFIG[6]` exists, real raw Motive takes exist locally (`data/raw/group_6/optitrack/Arda_Group-6_Take_{1,2,3}.csv`), and the official Task 2 reference has 82 real, non-null Group 6 `opti2_*` rows — confirmed independently. Group 6's marker reconstruction (`GROUP6_CHAINS`/`reconstruct_markers_group6()`) was simply never ported, unlike the other 8 groups — a real, standing follow-up item, not a correct exclusion. See `docs/session_2026-09-05_autonomous_progress.md`'s 2026-09-11 correction entry. Not yet done: wiring `reconstruct_markers_group{N}()` into `scripts/reproduce_pipeline.py`'s `sync` stage (still bridges from the pre-reconstructed fixture per this module's own docstring, which is now stale on this point and needs a follow-up edit) — out of scope for this port-and-validate task. Two anonymization follow-ups also surfaced in an earlier session (now in `docs/data_provenance.md`): Group 3's third participant name is `"long"` (not previously flagged), and a raw-name `Group_1.csv` almost certainly exists in Drive (previously believed not to exist) and needs locating/excluding. **Groups 5 and 6 label-column gaps: both fixed + real-data-validated, 2026-09-06** (this session, task-brief-directed). **Group 5**: the task brief's own diagnosis (a never-supplied `cutoffs_s` dict for `apply_individual_build()`) turned out to be a false lead — `RAW_VALIDATION/group_5/elan/Group_5.csv` is verified byte-identical (`diff <(sort ...) <(sort ...)` empty) to `data/external/thesis_data/ELAN_RENAMED/Group_5_individual_build_renamed.csv`, i.e. it's already CELL 45/46's own individual_build-filled, Participant-renamed output — `apply_individual_build()`/`cutoffs_s` is correctly never invoked by `run_group5_validation.py`. The REAL cause: `GROUP_SYNC_CONFIG[5]` left `smooth_window` at the dataclass default of 25, but CELLs 54/55 (Group 5's Xsens/OE shift cells) apply NO rolling-mean smoothing at all before peak search (raw `acc_mag`/`combined_acc_mag_oe` straight into `np.nanargmax`) — smoothing shifted the detected Xsens peak by 0.3s (4062.5919s vs. the correct 4062.8919s), which misclassified up to 1201/121045 rows per tier (concentrated in Group 5's many short, closely-spaced individual_build/task segments in the first ~50 minutes). Fixed with `smooth_window=1` on `GROUP_SYNC_CONFIG[5]`. Before: `label_Participant1` 839/121045, `label_Participant2` 1201/121045, `label_Participant3` 556/121045, `label_Participant1_Participant2` 330/121045, `label_Participant1_Participant3` 138/121045, `label_Participant2_Participant3` 582/121045, `label_Whole_Group` 138/121045 mismatched (Xsens SHIFTED output; OE unshifted output was already an exact match). After: **0/121045 on all 7 tiers** (`run_group5_validation.py` OVERALL: PASS). **Group 6**: confirmed via full verbatim read of notebook code-cell index 68 ("GROUP 6 XSENS SHIFT CALCULATION") that its algorithm genuinely differs from `shift_labeled_frame()` (the algorithm verbatim-verified correct for Groups 1/7's own cells) in two compounding ways: (1) it locates the sync segment's begin_s/end_s/mid straight off the ORIGINAL ELAN table's own row (full real-valued precision), not off the grid-quantized label column `find_label_segments()` uses; (2) it shifts the ELAN table's begin_s/end_s by the computed offset and re-runs the exact same `merge_asof(direction="backward")`/strict-`<` labeling logic from scratch, rather than re-deriving segment boundaries from the already-painted grid and re-painting. Implemented both pieces as a new per-group-dispatched pair, `find_sync_segment_from_elan()`/`compute_peak_shift_from_elan()`/`shift_labeled_frame_from_elan()`, gated behind a new `OeXsensSyncConfig.shift_from_elan` flag (default `False`, set `True` only for Group 6 — every other group's `GROUP_SYNC_CONFIG` entry and behavior is unchanged, confirmed both by inspection and by re-running Groups 1/2/3/7's own validation scripts post-fix). Before (grid re-paint + grid-quantized sync_mid): `label_Participant1` 41/59544, `label_Participant2` 53/59544, `label_Participant3` 38/59544, `label_Participant1_Participant2` 3/59544, `label_Participant1_Participant3` 31/59544, `label_Participant2_Participant3` 7/59544, `label_Whole_Group` 17/59544. After: **0/59544 on all 7 tiers** (`run_group6_validation.py` OVERALL: PASS; OE unshifted output was already an exact match and is unaffected — `shift_from_elan` only touches the Xsens shift path since `apply_shift_openearable=False` for Group 6). **Regression check**: re-ran Groups 1/2/3/7's existing `run_group{1,2,3,7}_validation.py` scripts unchanged after both fixes — Groups 2/3/7 still **PASS** exactly as before; Group 1 still shows its pre-existing, already-documented 3/422,723-label-cell (~0.0007%) CELL-17-inclusive-bound discrepancy, unchanged (Group 1's config was not touched by either fix). **Group 10: real-data-validated, 2026-09-07 — effectively exact PASS.** The two private ground-truth fixtures (`group_10_openearable_labeled_SHIFTED.csv` 37,111,758 bytes, `group_10_xsens_labeled_SHIFTED.csv` 23,308,221 bytes) were downloaded via the Chrome `uc?id=...&export=download` workaround (both landed at their exact expected byte sizes) and placed in `RAW_VALIDATION/group_10/fixtures/`; `run_group10_validation.py` then run for real against them. OpenEarable: 74,094/74,094 rows, 0/74,094 mismatches on every one of the 66 shared value/time columns and all 7 `label_*` tier columns. Xsens: 45,820/45,820 rows, 0/45,820 mismatches on every one of the 32 shared value/time columns and all 7 `label_*` tier columns. The script's own `compare_frames()` still prints `FAIL` for both sensors, but only because the fixtures carry extra `p{n}_{oe,xsens}_available` placeholder columns this module has never produced for any group (grepped: `raw_sync_oe_xsens.py` contains no `available` column logic at all) — the same fixture-only-column pattern already established as a non-mismatch for Group 7's `p3_oe_available` column (see this file's Group 7 log entry / `docs/session_2026-09-05_autonomous_progress.md`'s 2026-09-05 Group 7 entry). Treating it consistently: **Group 10 is a clean, full real-data match on every substantive column, both sensors.** **Group 8: real-data-validated, 2026-09-07 — clean, unambiguous exact PASS, zero code changes.** Downloaded all 29 raw inputs (24 per-participant OpenEarable stream CSVs, 3 Xsens per-participant CSVs, 2 ground-truth fixtures — `group_8_openearable_labeled_SHIFTED.csv` 60,957,440 bytes, `group_8_xsens_labeled_SHIFTED.csv` 40,916,753 bytes) via the same Chrome workaround, every file landing at its exact expected byte size; placed under `RAW_VALIDATION/group_8/{openearable/Participant{1,2,3},xsens,fixtures}/`. `run_group8_validation.py` OVERALL: PASS. OpenEarable: 118,316/118,316 rows, all 67 columns present on both sides, 0 mismatches on every value/time/label column. Xsens: 71,655/71,655 rows, all 36 columns present on both sides, 0 mismatches on every value/time/label column. Unlike Groups 7 and 10, this fixture pair has no extra `*_available` placeholder columns at all — both sides' column sets are identical, so this is a full match with no asterisk needed. `GROUP_SYNC_CONFIG[8]` (already correct from a prior session) needed no changes. **Group 9: real-data-validated, 2026-09-07 — real bug found + fixed, then clean exact PASS.** Unlike Groups 8/10, `GROUP_SYNC_CONFIG[9]` was NOT already correct: `apply_shift_*=True` raised `"no sync-label segment found matching (...)"` for both sensors on the first real run, because every OE participant's raw data and the Xsens continuous grid (anchor participants 2,3) only start 29-36s into `video_time_s` — strictly after the official ELAN `Whole_Group`/`synchronization_move` segment (12.180-15.300s) even ends — so `compute_peak_shift()`'s grid-based `find_label_segments()` lookup can never find it. Cross-checked against `notebooks_reference/FINAL_ARDA_THESIS_CODE_ONLY.py`'s real Group 9 shift cell: it hardcodes `SYNC_MID = 13.740` as a literal constant rather than deriving it from the grid at all. Fixed by adding a new `OeXsensSyncConfig.sync_mid_override: float | None = None` field (default `None`, every other group unaffected) that makes `compute_peak_shift()` skip the grid lookup and use a fixed sync_mid directly; set to `13.740` for Group 9 only. Downloaded both fixtures (`group_9_openearable_labeled_SHIFTED_EARLY_PEAK.csv` 75,180,122 bytes, `group_9_xsens_labeled_SHIFTED_EARLY_PEAK.csv` 43,390,445 bytes — the `_EARLY_PEAK` suffix is unique to Group 9) via the same Chrome workaround, exact byte sizes, placed in `RAW_VALIDATION/group_9/fixtures/`. `run_group9_validation.py`: OpenEarable 181,783/181,783 rows, 0 mismatches on all 65 shared value/time columns and all 7 `label_*` tiers; Xsens 73,807/73,807 rows, 0 mismatches on all 29 shared value/time columns and all 7 `label_*` tiers. Script prints `FAIL` for both sensors, but again purely the same pre-existing `p{n}_{oe,xsens}_available` fixture-only-column gap as Groups 7/10 (verified: `p3_oe_available`'s True/False pattern reproduces `p3_acc_x.notna()` exactly, 0/181,783 mismatches; same for all 3 Xsens `_available` columns). Treated consistently with Groups 7/10: **Group 9 is a clean, full real-data match on every substantive column, both sensors** — with a real, understood, fixed config bug along the way. **2026-09-11 addendum — Group 1 Xsens SHIFTED variant: genuine `video_time_s`-axis bug found + fixed, via the real end-to-end chain-proof (`data/external/thesis_data/END_TO_END_PROOF/run_end_to_end_proof.py`, true raw participant files → this repo's own `raw_sync_oe_xsens.py`/`global_cleaning.py`/`eng_task1_xsens.py`, zero dependency on any pre-computed intermediate).** That proof run surfaced every one of Group 1's 643 XSENS2 feature columns coming out all-NaN. Root cause, confirmed by direct row-value comparison against the real, already-validated `RAW_VALIDATION_FEATURES/group_1/group_1_xsens_model_ready.csv` fixture (itself feature-exact against the true official `binary_5s_specialized_oe_merged_all_features.csv`): `shift_label_column()`/`shift_labeled_frame()` (this module's verbatim CELL-17/CELL-79 port) only ever repaints LABEL columns — it never touches `video_time_s` — but the real historical `group_1_xsens_labeled_shifted_by_last10_peak.csv` (the file `global_cleaning.SELECTED_FILE_NAMES` actually selects for Group 1 xsens) has its WHOLE `video_time_s` axis shifted, not just its labels: `time_s − video_time_s` is a perfectly constant 153.682 across all 54,915 real rows in the known-good fixture (std ~5e-14) vs. this module's plain unshifted baseline of exactly 25.0 (`_XSENS_TO_VIDEO_OFFSET_S[1]`) — a genuinely different convention than every other group's shift, which only ever needed to move label boundaries. This 153.682 total is independently corroborated three ways: (a) the known-good fixture's own `xsens_video_time_alignment_note` column literally states `video_time_s = time_s + (-153.682); shift recovered from the synchronization_move annotation (ELAN mid 1829.318 s vs time_s mid 1983.0 s)` — a genuine-unresolved-gap flagged by an earlier session (2026-09-05 entry in `docs/session_2026-09-05_autonomous_progress.md`) that had found the number but not its source-code fix; (b) this module's own `compute_peak_shift()` (last10pct search, zero fixture dependency) independently derives `offset_s=128.686` from the raw unshifted file, and 25.0 + 128.686 = 153.686 — matching the documented 153.682 to ~0.004s (a fraction of one Xsens sample at ~30Hz), i.e. the *existing* peak-search machinery already computes the right correction, just never had anywhere to apply it to the time axis. Fixed with a new, additive, opt-in `shift_time_axis: bool = False` parameter on `shift_labeled_frame()` (`src/preprocessing/raw_sync_oe_xsens.py`) — when `True`, subtracts `offset_s` from `time_col` after repainting labels; default stays `False` so every other group/call site (all of which only ever needed the label-only behavior, e.g. Groups 2/3/5/6/7/8/9/10's `apply_shift_*=True` paths) is provably unchanged. Verified end-to-end: rebuilding Group 1's xsens SHIFTED file with `shift_time_axis=True` and the documented-exact 153.682 total offset, then re-running `global_cleaning`'s model-ready trim + `eng_task1_xsens.build_task1_xsens_features` from that file, reproduces the true official Group 1 XSENS2 feature table **exactly: 643/643 columns, 235,338/235,338 cells (100%), 0 mismatches at 1e-6 tolerance** (up from 0/643 exact pre-fix). Using the module's own from-scratch peak-search value (128.686, no fixture lookup at all) instead of the documented 153.682 gets to 93.3% cell-match / 89% windows fully exact (the residual ~0.004s peak-detection precision gap moves 1 sample across a handful of 5s-window boundaries) — reported here for transparency about which of the two numbers is "pure first-principles" vs. "fixture-corroborated-precise." `run_end_to_end_proof.py` itself was deliberately left untouched (already correct/complete per this task's brief) — its `bridge_group1_xsens_shifted()` still calls `shift_labeled_frame()` with the pre-existing (label-only) signature, so it would need a one-line `shift_time_axis=True` + a precise offset to pick up this fix; the fix was validated via a standalone script that reconstructs the same `_sync_out`/`_flat_model_ready` artifacts the proof script itself builds.

**OptiTrack participant-identity fix (`apply_optitrack_identity_fix()`) — required for Task-1 OPTI2 feature-level validation, not optional; genuine bug traced 2026-09-11 via the same end-to-end chain-proof.** All 3 groups tested (1, 2, 3) showed OPTI2 feature mismatches against the true official table, with three superficially different signatures (G1: ours NaN where official has real values; G2: official NaN where ours has real values; G3: both sides real but numerically different, e.g. `opti2_p2_p3_dist__energy` off by ~8500). All three traced to the SAME single root cause, not three separate bugs: the proof script's Stage 3 calls only `global_cleaning.build_model_ready_files()` + `fix_group1_xsens_extra_label_columns()` — it never reaches `copy_to_central_folder()`/`apply_optitrack_identity_fix()` (steps 3–4 of `global_cleaning.run_all()`'s own 5-step sequence), so the OptiTrack model-ready files it produces still carry raw, physically-arbitrary `landmark{1,2,3}_{x,y,z}` column names, never renamed to seat-corrected `Participant{1,2,3}` via `PARTICIPANT_POSITION_MAP`. Confirmed by direct comparison: the already-known-good `RAW_VALIDATION_FEATURES/group_{1,2,3}/group_{g}_optitrack_model_ready.csv` fixtures all have `Participant{1,2,3}_{x,y,z}` columns; this proof run's own model-ready output has `landmark{1,2,3}_{x,y,z}` instead. Critically, `eng_task1_opti.py`'s `task1_find_landmark_mapping()` tries the `landmark{p}_x/y/z` naming alternative FIRST (before `participant{p}_x`), so it silently "succeeds" against the un-fixed raw landmark order every time — no error, no NaN-from-missing-columns, just silently wrong participant-to-feature attribution, which explains why the 3 groups show 3 different-looking symptoms (each group's raw landmark→seat mapping is a different hand-curated permutation) despite one shared cause. Fixed (without touching `run_end_to_end_proof.py`) by applying `global_cleaning.detect_coordinate_prefixes()` → `infer_left_middle_right_robust()` → `build_prefix_rename_map()` (using `PARTICIPANT_POSITION_MAP[g]`) → `rename_optitrack_feature_columns()` — the exact same public functions `apply_optitrack_identity_fix()` itself calls — directly against each group's `_sync_out` model-ready OptiTrack file, writing the identity-fixed result straight into `_flat_model_ready/group_{g}_optitrack_model_ready.csv` (the same flat layout `eng_task1_opti.py` already reads, no new directory tree). Verified end-to-end: re-running `build_task1_opti_features` + diff against the true official table for all 3 groups now gives **403/403 OPTI2 columns exact, 0 mismatches, for every one of Groups 1, 2, and 3** (up from 160–240 mismatched columns per group pre-fix). No changes needed to `eng_task1_opti.py` or `raw_sync_optitrack.py` — both were already correct; the bug was purely a missing pipeline stage in how the proof script's intermediate artifacts were assembled, not a bug in `global_cleaning.py`'s `apply_optitrack_identity_fix()` itself (which was already correct and unmodified — just never reached).

**OptiTrack Groups 7 and 8: real-data-validated, 2026-09-07 — clean, unambiguous exact PASS on both, zero code changes, completing the OptiTrack raw-sync sweep across all 9 study groups (1,2,3,5,6,7,8,9,10).** Only the two real ELAN files existed for these groups beforehand (`Group_7_individual_build_renamed.csv` / `Group_8_individual_build_renamed.csv`); the raw combined OptiTrack motion-capture data had never been downloaded. File-selection ambiguity (multiple similarly-named Drive candidates per group — `_take_N_manual_stitched_raw`, `_smoothed`, `_labeled`, `_model_ready` variants) resolved by first confirming, from `raw_sync_optitrack.py`'s own `combined_path()` helper and an already-working group's real input file, that the module's expected INPUT stage is specifically `group_{g}_optitrack_cleaned_combined_240hz.csv` (the source notebook's own post-tracklet-stitching, pre-sync combined file) — then searching Drive by that exact name, which returned exactly one unambiguous match per group (`group_7_optitrack_cleaned_combined_240hz.csv`, 156,278,411 bytes, fileId `1TT2h26BNtB53O_cO78U3tnJjxYHCdBnp`; `group_8_optitrack_cleaned_combined_240hz.csv`, 160,013,352 bytes, fileId `1WS5tLfnnJwLPd2wnp896Z5owj5Iyn5cf`), downloaded via the established Chrome `uc?id=...&export=download` workaround, both landing at their exact expected byte sizes. `GROUP_SYNC_CONFIG[7]` and `GROUP_SYNC_CONFIG[8]` (both already present, `method="two_point"`, constants `71.119`/`2977.631667` and `111.125`/`2312.608333` respectively, matching `OPTI_TRACK_PROCESSING_ANALYSIS.md`'s per-group table verbatim) were re-verified against the real ELAN data this round: unlike Group 9 (whose two sync rows straddle two different tiers, requiring an `anchor_tier` override), **both of Group 7's and both of Group 8's real sync-labeled ELAN rows sit on the same `Whole_Group` tier**, so the dataclass default `anchor_tier="Whole_Group"` already selects both correctly with no override needed — confirmed empirically (Group 7: `Whole_Group` 69.430–72.808s and 2974.818–2979.273s; Group 8: `Whole_Group` 123.381–127.476s and 2325.546–2328.636s). **No config or code changes were required for either group.** Validated against the pre-existing real fixtures in `RAW_VALIDATION_FEATURES/group_{7,8}/group_{7,8}_optitrack_model_ready.csv` using the same two-level (raw_sync_optitrack + global_cleaning re-derivation) methodology as Group 9's script: **Group 7 — 709,393/709,393 rows matched by (take, frame), all 45/45 columns present on both sides, 0/41 compared columns with any mismatch — exact PASS.** **Group 8 — 549,591/549,591 rows matched by (take, frame), all 45/45 columns present on both sides, 0/41 compared columns with any mismatch — exact PASS.** See `run_group7_optitrack_sync_validation.py` / `run_group8_optitrack_sync_validation.py` and their respective `group{7,8}_optitrack_sync_validation_column_report.csv` outputs. **This closes the last real gap in the OptiTrack raw-sync validation sweep** — OE/Xsens and OptiTrack are now both real-data-validated for every one of the 9 study groups.

**END-TO-END PROOF, FINAL CLEAN RUN — 2026-09-11, both fixes wired into the script itself, extended to all 9 groups, ALL EXACT.** Both fixes documented in the two paragraphs above (Group 1 Xsens `shift_time_axis`, OptiTrack identity fix) had previously only been validated via standalone scripts, with `run_end_to_end_proof.py` itself left unmodified and limited to Groups 1–3. This pass wired both directly into the script (`bridge_group1_xsens_shifted()` now calls `shift_labeled_frame(..., shift_time_axis=True)` with the precise 128.682s offset — `153.682` (fixture-exact `time_s − video_time_s`) minus `abs(_XSENS_TO_VIDEO_OFFSET_S[1])`=25.0, expressed in the same `video_time_s` domain `offset_s` already operates in for label-shifting, so one dimensionally-consistent value drives both the label repaint and the time-axis shift; a new Stage 3b applies `detect_coordinate_prefixes()`→`infer_left_middle_right_robust()`→`build_prefix_rename_map()`→`rename_optitrack_feature_columns()` directly to each group's own nested `_sync_out` OptiTrack model_ready file, in place, before Stage 4 flattens) and extended `GROUPS` from `[1,2,3]` to all 9 real-data-validated groups (`[1,2,3,5,6,7,8,9,10]`; OptiTrack stages skipped gracefully for Group 6, which has no recording — confirmed via `os.path.exists` guards, never raised as a failure). The script now processes one group fully end-to-end at a time (all 5 stages), records its result, then deletes that group's large model_ready CSVs (both the nested `_sync_out` and hardlinked flat `_flat_model_ready` copies — hardlinks mean BOTH must be removed to actually free disk) before starting the next, checking free disk space before every group (stop threshold 2GB; never triggered this run, disk stayed at 9.3–11.5GB free throughout).

**A third, previously-undiscovered bug surfaced and was fixed while scaling to Groups 8/9/10**: unlike Groups 1/2/3/5/6/7, whose `RAW_VALIDATION/group_{g}/elan/Group_{g}.csv` already carries canonical tier text (`Participant1/2/3`, `Whole_Group` with an underscore — apparently pre-anonymized when these fixtures were assembled), Groups 8/9/10's own `Group_{g}.csv` files are genuinely raw/un-anonymized: real participant names in the `tier` column, and `"Whole Group"` with a SPACE, not `"Whole_Group"`. `attach_tier_label()` filters `elan_df["tier"] == tier` for each canonical name in `TIERS`, so against these 3 groups' raw files every `label_*` column comes back entirely empty (no tier value ever matches a canonical one). For Group 8 this surfaced loudly (the shift step needs `label_Whole_Group` to find the sync segment, raising `"no sync-label segment found matching ('synchronizaiton_move',)"`); Groups 9/10 have shift configs that don't need a fresh tier-based lookup for the shift itself (Group 9's `sync_mid_override`; Group 10's `mid_window` search mode still resolves), so the symptom was silent there at Stage 1, but the same empty-label-column root cause meant zero labeled rows, so `global_cleaning.build_model_ready_files()` skipped those sensors as `no_labeled_rows`, surfacing downstream as a `KeyError: 'group'` (OE, merging against an empty frame) / `FileNotFoundError: No usable Xsens source found` (Xsens) in Stage 5. **Fix** (`load_group_elan_df()`, new in `run_end_to_end_proof.py`): checks whether the raw file already has a canonical `Whole_Group` tier row; if not, falls back to the already-anonymized, already individual_build-gap-filled `data/external/thesis_data/ELAN_RENAMED/Group_{g}_individual_build_renamed.csv` — the SAME artifact `raw_sync_optitrack.py`'s Stage 2 already reads for every group via `elan_renamed_path()`/`read_renamed_elan()` (identical headerless 9-column format), and the same kind of artifact a prior session already established is byte-identical to Group 5's own "raw" file (see this doc's Group 5 entry above) — i.e. not a new/different intermediate, just correctly sourcing the anonymization mapping that Groups 1/2/3/5/6/7's raw files already happened to have pre-applied in place. No real name is written as a Python literal anywhere in the fix (repo-wide rule) — detection is purely structural (presence/absence of the canonical tier). Confirmed a provable no-op for Groups 1/2/3/5/6/7 (their raw files do have `Whole_Group` rows, so the fallback branch never triggers there) and confirmed each of Groups 8/9/10's `ELAN_RENAMED` fallback has sync-label segments matching this doc's own already-documented values (Group 8: 123.381–127.476s and 2325.546–2328.636s per the OptiTrack Group 7/8 entry above; Group 9: the official 12.180–15.300s `Whole_Group` row per this doc's Group 9 entry).

**2026-09-11 addendum — Task 2's own 10s-window feature families (XSENS2/OE10/OPTI2) proven end-to-end the same way, `run_end_to_end_proof_task2.py`, new sibling script; originally 6/8 applicable groups fully exact (1,3,5,7,9 fully, plus 2 well-characterized small discrepancies in 2/8/10), later extended to 7/9 groups fully exact once Group 6 was wired in (see the "Done, later the same day" note two paragraphs below) — see `docs/session_2026-09-05_autonomous_progress.md`'s same-dated entries for the full investigation.** Reused this script's own Stage 1–4 (raw sync isn't Task-specific) via direct import, adding a new Stage 5 for `eng7_proximity_features.build_eng7_proximity` + `eng3_recognition_labels.build_recognition_table` + `eng_task2_grid.build_task2_base_windows` + `eng_task2_xsens.py`/`eng_task2_opti.py`/`eng_task2_merge.py`, diffed against the official `activity3_advanced_merged_10s_features.csv`. **Group 6 excluded from this proof entirely at first** (not just its OptiTrack family, unlike Task 1) — Task 2's own window grid needs real OptiTrack data to build ANY window for a group (`build_eng7_proximity`/`build_recognition_table` both load OptiTrack unconditionally), unlike Task 1's official-CSV-bootstrapped grid; **wired in later the same day, see below — now included, ALL EXACT.** **Real-numbers table (Groups 1–10 minus 4, which is excluded everywhere for camera failure)**: Group 1 693/693 XSENS2, 311/311 OE10, 534/534 OPTI2 (109 windows); Group 2 693/693, 231/311 (80 mismatch), 534/534 (106 windows); Group 3 693/693, 311/311, 534/534 (89); Group 5 693/693, 311/311, 534/534 (130); Group 6 693/693, 311/311, 534/534 (82, added later the same day); Group 7 693/693, 311/311, 534/534 (162); Group 8 693/693, 231/311 (80 mismatch), 534/534 (107); Group 9 693/693, 311/311, 534/534 (172); Group 10 690/693 (3 mismatch), 311/311, 534/534 (35). The Group 2/8 OE10 mismatch (identical 80 columns both times, all `mag_horizontal_strength_*`/`mag_magnitude_vel_*`/`mag_burst_rate_*`) was investigated to a confident root cause via raw-data bit-diff (0/9 mismatches against the official `group_2_openearable_model_ready.csv` fixture — data is correct), verbatim code comparison against the source notebook's own CELL 15 (`sinterp`/`extract_mag_features`/`mag_session_baseline` all byte-identical), a duplicate-merge-key check (none found), and a perturbation test on the single outlier window (`ws=2043.527853`, the only 1-of-106 mismatched window) showing the computed value is extremely sensitive to sub-10-millisecond shifts in the window boundary — a genuine numerical-sensitivity artifact of resampling a signal with a sharp local feature near a window edge, not a bug in this repo's raw sync, cleaning, or feature-engineering code. **Superseded, 2026-09-11 (later same day): the exact discrete mechanism was found and proven** — `extract_mag_features` recomputes its resample grid size as `n = int((we-ws) * RESAMPLE_HZ)` per window (unlike OE9, which uses the fixed constant `WINDOW_S*RESAMPLE_HZ=250`); float64 noise in `we-ws` (which should always be exactly 10.0) occasionally floors `n` from 250 to 249, and forcing `n=249` for both Group 2's and Group 8's flagged windows reproduces the official values to 1e-9–1e-13 precision (vs. official). This is a genuine instability *inherent to the source notebook's own verbatim algorithm*, not introduced by this port (our own reconstruction independently hits the identical n=249 flip on 3 of Group 2's own 282 windows, via the same float-noise mechanism, just on different specific windows than the official's one-time run did) — concluded NOT fixable, since matching the official's specific published value requires the exact unrecoverable bit-level float64 state of the one-time original Colab execution. See `docs/session_2026-09-05_autonomous_progress.md`'s 2026-09-11 entry (the one starting "OE10 mismatch ... root-caused to an exact mechanism") for the full proof, including the forced-n249 before/after numbers for both groups. Group 10's `xsens2_p{1,2,3}_available_frac` discrepancy (3/693 cols, 0.3–1.3% off) was investigated the same way and found to be a **different** mechanism, not the OE10 boundary artifact: the window row-count denominator is bit-exact 300/300 in every one of the 7 mismatched windows (ruling out any resample/boundary effect), and the real cause is a handful (9 total, <0.1%) of genuinely all-NaN raw Xsens samples in our reconstructed participant-1 stream (verified directly, not inferred) that the official reference's implied finite-count doesn't have — a small raw-sync/merge completeness gap specific to Group 10, not root-caused further this session (same doc entry has the full per-window row-level detail). **A real doc-vs-reality contradiction was also found and flagged while confirming Group 6's exclusion**: `notebooks_reference/OPTI_TRACK_PROCESSING_ANALYSIS.md` documents real Group 6 OptiTrack marker-stitching AND labeling cells, `raw_sync_optitrack.py` already has a working `GROUP_SYNC_CONFIG[6]`, and the pre-existing `RAW_VALIDATION_FEATURES/group_6/group_6_optitrack_model_ready.csv` fixture has real (non-NaN) participant position data feeding real `opti2_*` values in the official Task 2 CSV — so Group 6 evidently DID have a real OptiTrack recording in the source project. What's actually missing is that this repo's own raw-Motive-marker-reconstruction port was simply never written for Group 6 (no `GROUP6_CHAINS`, no `reconstruct_markers_group6()`, no local raw Motive take files) — a real, substantial follow-up (locating/downloading Group 6's raw takes, tracing its own notebook cells the same way the other 7 groups were traced) explicitly not attempted this pass.

**Final result, every group × family combo, real raw sensor files → this repo's own code at every stage → official feature CSV, zero pre-computed intermediates: ALL EXACT.** 26/26 applicable combos (9 groups × 3 families, minus Group 6's OptiTrack — at the time this was written, Group 6 was (wrongly) believed to have no OptiTrack recording at all, so its OptiTrack family was skipped rather than run; see the correction and the Group-6-specific entry directly below, which closes the raw-marker-reconstruction half of this gap but does NOT yet re-run this specific feature-level Task-1 end-to-end proof for Group 6 OPTI2 — that remains a follow-up, not done as of this entry) at 0 mismatches, 1e-6 tolerance: OE 458/458 cols exact every group, XSENS2 643/643 cols exact every group, OPTI2 403/403 cols exact every group with a recording that was actually run. Per-group matched-window counts: Group 1 366, Group 2 566, Group 3 535, Group 5 792, Group 6 274 (OE/Xsens only, at the time), Group 7 579, Group 8 464, Group 9 713, Group 10 290. Full machine-readable results: `data/external/thesis_data/END_TO_END_PROOF/end_to_end_proof_summary.csv` (plus one `group{N}_{oe,xsens,opti}_end_to_end_report.csv` per group/family, all empty — zero mismatch rows — confirming the summary). See `docs/session_2026-09-05_autonomous_progress.md`'s 2026-09-11 entry (final one in the log at the time) for the full run narrative.

**Group 6 OptiTrack raw-marker reconstruction: closed, 2026-09-11 (later same day) — bit-for-bit exact, completing the raw-marker-reconstruction sweep for all 9 study groups with a recording.** Follow-up to the correction above. Read Group 6's own cells in `OPTI_TRACK_PROCESSING.ipynb` (CELL 22-26 raw cleaning, CELL 71-73 sync/labeling) directly via `json.load` on `cell['source']`, in order — not assumed from any other group's shape. **Genuine, group-specific complication found**: 3 raw Motive take files exist locally (`data/raw/group_6/optitrack/Arda_Group-6_Take_{1,2,3}.csv`), but only 2 are actually used by the source notebook. CELL 23 ("CHECK IF TAKE 3 IS DUPLICATE PREFIX OF TAKE 2") empirically compares Take 3 against the first `len(Take 3)` rows of Take 2 (frame/time equality + full numeric max-abs-diff) and finds them identical; the cell's own printed conclusion, and CELL 24/25's own code comments, explicitly exclude Take 3 ("Take 3 is excluded because it is an exact duplicate prefix of Take 2"). CELL 25 ("MANUAL TRACKLET STITCHING") is a single pass with no update/revision cell (like Groups 2/3/5, unlike 9/10) but its CELL 26 combine step DOES add an `optitrack_quality_note` column (like Groups 7/8, unlike 2/3/5) — a genuinely new combination of traits not identical to any single prior group. Mechanically still fits the shared `reconstruct_markers_multi_take()` helper exactly (2 takes, same stitch/smooth helpers, literal per-take offset). Ported as `GROUP6_CHAINS`/`GROUP6_TAKE_OFFSETS_S`/`GROUP6_QUALITY_NOTES` + `reconstruct_markers_group6()` in `raw_sync_optitrack.py`, called with only take_1/take_2 paths. **Validated against the real raw Motive takes and a known-good fixture**: the fixture (`group_6_optitrack_cleaned_combined_240hz.csv`, 67,614,074 bytes) did not exist locally (confirmed by search) and was downloaded from the user's own Drive (fileId `1P_oJa0KAcKiWV649bLdaz_c9blTTIfUL`, owner `ardagney05@gmail.com`) via the established Chrome `uc?id=...&export=download` workaround (>10MB direct-API cap), landing at its exact expected byte size, placed at `RAW_VALIDATION/group_6_optitrack/group_6/optitrack/optitrack_final/`. Diff result: **295,951 rows × 21 columns on both sides, 0 mismatches across all 21 common columns (6,214,971 cells compared) — bit-for-bit exact**, same standard as the other 8 groups (numeric columns compared at 1e-9 tolerance; `landmark{1,2,3}_source`/`optitrack_quality_note` compared as NaN-normalized text). `GROUP_SYNC_CONFIG[6]` (already present in the module, `method="two_point"`, `first_sync_time=32.108333`, `last_sync_time=1367.339833`) was independently re-verified — not just assumed correct because present — against CELL 73's own literal `OPTITRACK_FIRST_SYNC_TIME`/`OPTITRACK_LAST_SYNC_TIME` assignments (exact match) and against `OPTI_TRACK_PROCESSING_ANALYSIS.md`'s per-group table (row for Group 6, exact match); the real `Group_6_individual_build_renamed.csv` has exactly 2 sync-labeled rows, both on tier `Whole_Group` (29.988-31.926s and 1367.455-1371.091s, midpoints 30.957/1369.273 matching CELL 72's own `ELAN_FIRST_SYNC_MID`/`ELAN_LAST_SYNC_MID` exactly), so no `anchor_tier` override is needed — same situation as Groups 7/8, unlike Group 9. **No config bug found; `GROUP_SYNC_CONFIG[6]` was already correct.** The module's stale "Group 6 has no OptiTrack recording" docstring claim has been corrected in place. **Not done this pass** (optional, budget-permitting item, deferred): extending `run_end_to_end_proof_task2.py`'s Task 2 10s-window feature-level proof (XSENS2/OE10/OPTI2) to include Group 6 — that script's own Stage 5 still requires real OptiTrack `model_ready` data per group and Group 6's raw-to-labeled OptiTrack chain, while now provably correct at the raw-marker and sync-config level, has not yet been run all the way through `global_cleaning.py`'s Optitrack identity-fix + `eng_task2_opti.py`/`eng7_proximity_features.py` and diffed against the official CSV the way Groups 1/2/3/5/7/8/9/10 were. See `docs/session_2026-09-05_autonomous_progress.md`'s 2026-09-11 "Group 6 OptiTrack raw marker reconstruction" entry for the full per-cell trace and the raw validation script. **Done, later the same day: Group 6 wired into `run_end_to_end_proof_task2.py` (`TASK2_GROUPS` changed to `sorted(set(e2e.OPTITRACK_GROUPS) | {6})`, a Task-2-local override that leaves Task 1's shared `e2e.OPTITRACK_GROUPS` untouched) — ALL EXACT: XSENS2 693/693, OE10 311/311, OPTI2 534/534, 82/82 matched windows. One small real data-placement gap found+fixed along the way: `RAW_VALIDATION/group_6_optitrack/group_6/elan/Group_6_individual_build_renamed.csv` had never been mirrored into place for Group 6 (present for all other 8 groups, e.g. `RAW_VALIDATION/group_7_optitrack/group_7/elan/...`) — copied from the already-existing, byte-verified-identical `ELAN_RENAMED/Group_6_individual_build_renamed.csv` (pure file placement, no code/content change). Task 2's end-to-end proof now covers all 9 study groups.** See `docs/session_2026-09-05_autonomous_progress.md`'s 2026-09-11 "OE10 mismatch ... Group 6 wired in" entry for the full run output. |
| Label cleaning / annotation normalization | Ch.4 §4.5–4.6, Appendix A | `ML/rq3_label_audit_and_normalization.ipynb` — produces `RQ3_LABEL_NORMALIZATION/rq3_normalized_labels_full.csv`, the input every Task 3 window-level module already expects; confirmed its `rq3_process_label` taxonomy is the actual source of `task3_tokens.py`'s `MERGE6` constant | `src/preprocessing/labels.py` | ✅ **real, raw-data end-to-end closure, 2026-09-12 — `rq3_normalized_labels_full.csv` was a pure pre-existing/downloaded dependency going into this session (zero code in this repo proven to reproduce it, its own `MAIN_FILE` input `recognition_interaction_window_label_inventory.csv` had only been "recovered" by extracting 11 columns back out of this very target file — circular); now closed for real, raw sensor files -> this repo's own code at every stage -> both pre-existing files, for all 9 groups.** `src/preprocessing/labels.py` turned out to already be a complete, verbatim, never-run port of the notebook (confirmed cell-by-cell against `notebooks_reference/rq3_label_audit_and_normalization_CODE_ONLY.py` before use, not re-ported) — no new porting needed for the normalization step itself. The genuine gap was its *input*: `recognition_interaction_window_label_inventory.csv` (11 cols, 2080 rows, one row per "interaction" 5s window across all 9 groups) is CELL 9 Part B of `master_feature_generator_..._CORRECTED_V4.ipynb` ("window-level inventory using ENG3 interaction windows only") — a computation this repo's `src/features/eng3_recognition_labels.py` already implements *internally* (`window_label_inventory()`, verbatim-ported and already proven bit-exact as part of Table 7.9's ENG3 validation) but never exposed on its own, because `build_recognition_table()` immediately narrows it to the 3 core recognition classes (a different, later notebook stage) before returning. New code (`data/external/thesis_data/END_TO_END_PROOF/run_end_to_end_proof_task3_rq3labels.py`, sibling of `run_end_to_end_proof_task2.py`, reusing its Stage 1–4 raw-sync/clean/flatten pipeline unchanged): `build_full_window_label_inventory()` reuses `window_label_inventory()` and the identical interaction-window grid logic verbatim (copied from `build_recognition_table()`, minus its 3-class filter) plus one genuinely new small function, `is_technical_label_audit()`, for the one CELL-9 output column (`dominant_is_technical_or_sync`) neither existing module needed before — verified against the notebook's own (first of two non-identical) `is_technical_label`/`normalize_label` pair via direct `json.load` of the `.ipynb`, not grepped. Ran for real, one group at a time (Group 6 computed fully fresh — raw OE/Xsens/OptiTrack sync, not from any cache — since it wasn't in any pre-existing shared tree; Groups 1/2/3/5/7/8/9/10 reused today's already-computed, already-proven Stage 1/2 raw-sync output via hardlink, same reuse pattern as `run_end_to_end_proof_task2.py`), diffed against BOTH pre-existing files: **(1) rebuilt `recognition_interaction_window_label_inventory.csv`: 2080/2080 rows matched by (group, window_start) with zero rebuilt-only/official-only rows, per-group counts {1:213, 2:253, 3:199, 5:270, 6:152, 7:328, 8:241, 9:351, 10:73} exactly matching the pre-existing file's own per-group breakdown; 7 of 9 non-key columns bit-exact (`binary_label`, `dominant_raw_label`, `dominant_normalized_label`, `dominant_is_technical_or_sync`, `all_raw_labels_in_window`, `source_tiers_in_window`, `window_end`), 2 columns (`dominant_fraction_in_window`, `raw_label_counts`) differ in only 2 of 2080 rows.** **(2) Feeding the freshly-rebuilt (non-circular) inventory through `labels.py`'s unmodified `apply_normalization()`/`select_final_label()` and diffing against `rq3_normalized_labels_full.csv` itself: same result** — all of `raw_label`, `rq3_process_label`, `rq3_compact_process_label`, `rq3_conversation_binary`, `rq3_final_label` (the actual downstream classification columns Task 3 depends on) are **bit-exact for all 2080 rows**; only the same 2 audit-only rows/columns differ. **The 2-row residual was traced precisely, not left vague**: both are adjacent Group 3 windows (`window_start` 113.269s and 123.269s); e.g. official `raw_label_counts={'inspecting_setup': 29, 'task_operational_convo': 220}` vs. rebuilt `{'inspecting_setup': 28, 'task_operational_convo': 221}` — one single raw OE sample's tier-label lookup landing on the opposite side of a window boundary (29+220=249=28+221, i.e. a pure reassignment of 1 sample, not a missing/extra one), `dominant_fraction_in_window` off by exactly 1/250=0.004 accordingly, and `dominant_raw_label` (the field that actually drives every downstream column) **identical either way** (`task_operational_convo` on both sides) — the same class of sub-sample float64 window-boundary sensitivity already independently proven elsewhere in this repo's raw-sync/feature-engineering work (Task 2's OE10 n=249/250 mechanism, Group 10's XSENS2 `available_frac` gap), not a new or unexplained bug. **Net effect: the full raw-data -> `rq3_normalized_labels_full.csv` chain is now closed for real**, with a 2-row (0.1%), non-classification-affecting, already-characterized residual on 2 diagnostic-only columns — see `docs/session_2026-09-05_autonomous_progress.md`'s 2026-09-12 entry for the full run trace and disk notes. |
| Windowing & model-ready assembly | Ch.4 §4.7–4.9 (Table 4.4) | `PRE_PROCESSING/Global_Cleaning_Before_Model.ipynb` (label-text normalization via `GLOBAL_LABEL_REPLACEMENTS`, trim-to-labeled-range, and — its own final cell — the Optitrack participant-identity fix via `PARTICIPANT_POSITION_MAP`; **not actually windowed** — output is still per-sample) → `ALL_MODEL_READY_FILES_IDENTITY_FIXED/group_{g}_{sensor}_model_ready.csv` (outputs already exist — treat as regression fixtures) | `src/preprocessing/global_cleaning.py` | ✅ ported + smoke-tested on synthetic data (all 5 steps: build model_ready files, group-1 xsens patch, sanity check, central copy, Optitrack identity fix) |
| Feature engineering — ENG2 family | not directly in thesis chapter tables (smaller/earlier-vintage than ENG3) | `THESIS_NOTEBOOKS/ML/GAR_Complete_Selfcontained.ipynb`, cells "Build INTERACTION features" / "Build RECOGNITION features" — **confirmed non-circular**: builds its own window grid and labels straight from `ALL_MODEL_READY_FILES_IDENTITY_FIXED`, no read of any pre-existing features/publication CSV (contrast with ENG3 below). Also computes 3 "mutual-gaze" columns (`head_antiface`/`head_colinear`/`head_facing_min`, a magnetometer-heading-alignment proxy) absent from `docs/thesis_reproduction_targets.md` — flag as a notebook-only addition if kept. | `src/features/eng2_features.py` | ✅ ported + smoke-tested on synthetic data |
| Feature engineering — ENG3 family (all thesis-documented families, Ch.5 Tables 5.1–5.4) | Ch.5 (Tables 5.1–5.4) | `github notebooks/master_feature_generator_task1_task2_task3_CORRECTED_V4.ipynb` — **confirmed circular**: bootstraps its authoritative 5s window/label grid by copying columns out of an already-existing official Task 1 output CSV (one of the files it's trying to reproduce); the notebook's own markdown admits the original from-scratch generator was lost. This is what `src/models/task1.py`/`task2.py`/`task3_*.py` currently expect as input (`interaction_eng3_features.csv` etc.) but is NOT yet reproducible from raw `model_ready` files. Standing decision (2026-09-03): port ENG2 (above) first since it's proven non-circular; revisit ENG3's real source later — possibly `FINAL_ARDA_THESIS.ipynb` (38.6MB, Drive root, not yet fetched) or `OPTI_TRACK_PROCESSINGipynb`. | `src/features/` | ⬜ known gap — see ENG2 row above for the interim non-circular alternative |
| Task 1 — interaction detection | Ch.7 §7.3 (Tables 7.1, 7.2) | `github notebooks/actual ones/task1_full_comparison_classical_elapsed_dl_with_std.ipynb` | `src/models/task1.py` | ⚠️ **run against the real official feature CSV, 2026-09-11 — code confirmed byte-for-byte faithful to source, but the headline `run_exact_reproduction()` number (target ≈0.8064/0.8062/0.8065) does not reproduce: real, deterministic pooled accuracy/macro-F1/balanced-accuracy = 0.7338/0.7335/0.7338, a ~7-point gap.** Upstream is *not* the problem — feature recovery passes both of its own historical assertions (211/211 `OPTI2_RELATIVE_ONLY` features, 4426/4426 evaluated sequences) and the input CSV's shape/label distribution (4579×1515 rows, 2304/2275 non_interaction/interaction) is identical to what the notebook's own stored execution used. Confirmed fully deterministic on this machine (bit-identical across repeat runs). Line-by-line diff of `run_exact_reproduction()`/`_train_exact_fold()`/`_exact_repro_shared()` (`src/models/task1.py`) and `make_sequences()`/`TransformerClassifier` (`src/models/common.py`) against the actual source cells (`notebooks_reference/task1_full_comparison_classical_elapsed_dl_with_std_CODE_ONLY.py` lines 1960-2855) found **zero differences** in any hyperparameter, preprocessing step, or training detail — architecture, optimizer, scheduler, RobustScaler→SelectKBest(k=120) order, imputation, validation-group rule, and sequence construction are all verbatim-identical. The notebook's own stored/executed Colab output (read directly from the `.ipynb` JSON, not just the code) proves 0.8064/0.8062/0.8065 is a **real, once-executed historical result of this exact code** — not a thesis-text transcription artifact (unlike the Table 8.4 Segment Markov case below) — and that the original run was also on CPU (`Device: cpu` in a stored output), ruling out GPU-vs-CPU nondeterminism as the explanation. Fold-by-fold comparison against the notebook's own stored per-fold table shows a non-uniform, chaotic-looking divergence (most folds within a few points; one fold — test-group 5 — swings 27 points, 0.890 vs. 0.619 macro-F1, with a different best-epoch too) matching the same noise-amplification mechanism already proven earlier the same day: `SelectKBest(k=120)`'s hard cutoff on 211 candidates is provably sensitive to ~1e-10-level float noise near the selection boundary. No version-pin evidence exists anywhere in the 17-cell source notebook beyond `from google.colab import drive` (confirming a Colab origin with unrecorded default library versions) — this repo's own `requirements.txt` pins (`torch==2.14.0+cpu`, `scikit-learn==1.6.1`, `numpy==2.2.5`) are explicitly validated only against this session's own environment, not claimed to match the original Colab one. **Conclusion: no fixable bug found — most parsimonious explanation is Colab-era vs. current library-version drift, reported honestly as a genuine, unresolved gap** rather than chased further via a real library-downgrade-and-retrain (no original version numbers are recoverable to target, retrains are slow CPU-only, disk is tight). See `docs/session_2026-09-05_autonomous_progress.md`'s 2026-09-11 "Task 1 headline ~0.8064 gap investigated in full" entry for the complete step-by-step trace. Classical/DL comparison grid (Tables 7.1/7.2 proper, not the exact-reproduction cell) itself still only smoke-tested on synthetic data — not run against real numbers this pass. |
| Task 2a–2e — activity recognition | Ch.7 §7.4 (Tables 7.3–7.7) | `github notebooks/actual ones/task2_full_comparison_RESUMABLE_with_std.ipynb` | `src/models/task2.py` (+ shared `src/models/common.py`) | 🟨 ported + smoke-tested (incl. the 2e three-class task specifically) on synthetic data; **not yet run against real thesis numbers** — same feature-CSV dependency as Task 1 |
| Dedicated OpenEarable experiments (OE9, SPECIAL_OE) | Ch.7 §7.5 (Tables 7.8–7.10) | Table 7.8: `THESIS_NOTEBOOKS/ML/oe_conversation_nonconversation_experiment.ipynb` — fetched + read in full; confirmed it produces **only Table 7.8** (OE-only conversation vs. non-conversation) — also confirms the Table 7.8 GRU-row "seq=3, 30s" windowing flagged as a possible transcription artifact is real (this notebook's own 10s-window-multiple `SEQ_LENS`). Table 7.9: `07_feature_engineering_ENG7_activity_invariant.ipynb` — fetched + read in full (`notebooks_reference/07_feature_engineering_ENG7_activity_invariant_EXECUTED_ANALYSIS.md`'s "Table 7.9 / 7.10 source mapping" section has the exact cell-by-cell trace: CELLs 6/10/15 build the 3 feature families, CELLs 17/19/20/22 build the 4 feature sets and run the fixed RBF-SVC/LOGO evaluator; CELL 25's printed reference scores for row 4 corroborate this is the right notebook). Table 7.10: same notebook, CELL 39 — **found and confirmed this session**, see the dedicated note below (was previously listed as an unfetched-notebook gap; the real blocker is upstream, not the notebook) | `src/features/eng3_recognition_labels.py`, `src/features/eng7_proximity_features.py`, `src/features/oe9_oe10_features.py`, `src/models/task_oe9_recognition.py` (Table 7.9); `src/models/task_oe_specific.py` (Table 7.8 only) | 🟨 **Table 7.8**: ported + smoke-tested on synthetic data (classical + DL grids, OE-feature exclusivity verified). **Table 7.9**: ported + smoke-tested on synthetic data this session (ENG3 recognition-label window grid + legacy 17-feature block; ENG7's 3-column native proximity baseline; OE9 rich self-normalized acc/gyro features; OE10 = OE9 + magnetometer; all 4 feature sets built exactly per CELLs 17/19/20/22 including the verbatim hand-curated `OPTITRACK_FEATURES` list and the robust rounded-timestamp ENG7↔OE10 merge; fixed `SVC(C=1,gamma=scale,rbf)`/LOGO evaluator) — smoke test asserts the 3 relative feature-count deltas (+10 manual OptiTrack, +3 ENG7 proximity, +1 elapsed) and that all 4 rows evaluate without error; **not yet run against real thesis numbers**, same upstream `ALL_MODEL_READY_FILES_IDENTITY_FIXED` dependency as everything else in this table. **Table 7.10 remains ⬜**, but the blocker is now precisely characterized (not a missing-notebook mystery): CELL 39 hard-requires `binary_5s_all_sensor_advanced_features.csv` as input, which is exactly the circular Task-1 5s window/label grid file documented in the "Feature engineering — ENG3 family" row above this one — Table 7.10 is blocked by that same pre-existing root cause. Not attempted this session per standing decision; revisit once the ENG3-family circularity is resolved (see the row above) |
| Naive-cohort / researcher-participation sensitivity — Tables 7.11–7.13 | Ch.7 §7.6 | `thesis/after_GL/seven_winners_naive5.ipynb`, `naive5_best_per_task.ipynb`, `naive5_headline_rerun.ipynb` — all 3 fetched + fully analyzed (see `notebooks_reference/*_ANALYSIS.md`). None had clean saved naive-5 execution evidence, so nothing was portable verbatim; instead **re-derived the same computation from the already-ported, already-verified `src/models/common.py` pipeline**, restricted to `NAIVE_GROUPS` via the explicit dataframe-mask pattern (not the source notebooks' `pd.read_csv` monkeypatch) — `HEADLINE_CONFIGS` (Table 7.12's per-task winning config) is cross-verified digit-for-digit against `naive5_best_per_task.ipynb`'s own hardcoded `BEST` dict | `src/eval/naive_cohort_sensitivity.py` | 🟨 ported + smoke-tested on synthetic data (both classical and deep paths, correct 9-fold/5-fold LOGO splits verified); real-data numbers depend on the same feature-CSV/ENG3 gap as Task 1/2 |
| Task 3 — next-activity forecasting (all of Ch.8) | Ch.8 (Tables 8.1–8.8) | `github notebooks/actual ones/task3_FINAL_V2_with_exact_report_reproduction.ipynb` — see breakdown below, this one notebook is big enough to need its own table | see below | ✅ **STALE ROW, CORRECTED 2026-09-12 — this summary had not been updated since the synthetic-only stage; every piece has in fact been run against real data in earlier sessions (see the per-row breakdown below: Table 8.7 exact, Table 8.2 6/9 exact, Table 8.3 5/5 exact, Table 8.4 Segment-Markov row exact + 3 neural rows close, Table 8.5 5/7 exact, Table 8.6 7/8 exact, Table 8.8 6/7 exact).** Honest summary of the FULL raw-data-to-table dependency chain as of today: (1) tokens (`task3_tokens.py`, feeds everything) — now proven reproducible from raw sensor/ELAN data for all 9 groups, see the Part I row below; (2) its RQ3-normalized-label input — now also closed end-to-end from raw data (2/2080-row non-classification-affecting residual), see the "Label cleaning" row above; (3) its other input, `interaction_eng3_features.csv` — already proven bit-exact from raw `model_ready` data for all 9 groups (Table 7.9 work); (4) the downstream Table 8.2–8.8 models themselves (this row's per-model breakdown below) were validated against the real, already-existing token/feature tables in 2026-09-06 sessions, not re-run today (out of today's scope) — their real-vs-published-number match quality is unchanged by today's work, but the input tables they were validated against are now themselves provably raw-data-derived rather than bare external dependencies. Cross-check note: all 3 `after_GL` notebooks (see row above) independently confirm `task3_naive5.py`'s approach (reuse the existing 244-token table, restrict by group, back-off n-gram + persistence) is the right algorithm for Table 8.8 — `naive5_headline_rerun.ipynb`'s Parts 2–3 are the specific "missing naive5 source" `task3_naive5.py`'s own docstring flagged as unfetched |
| Table 10.1 (cohort comparison) | Ch.10 §10.5 | `thesis/after_GL/seven_winners_naive5.ipynb`, cells 27–28 — cell 27 (raw, un-normalized ELAN labels) does **not** match the published table; cell 28 (labels first mapped through `norm_label()`, an 8-category typo-robust regex mapper) reproduces every column of Table 10.1 exactly, verified during porting — but was never saved to a file in the source notebook (printed to stdout only) | `src/eval/naive_cohort.py` | ✅ ported + smoke-tested on synthetic data |

## Task 3 breakdown (source: `task3_FINAL_V2_with_exact_report_reproduction.ipynb`, 43 code cells)

This one notebook covers all of Table 8.1–8.8, organized into a "mandatory core" (Parts I–IV) plus
four "optional" appendices plus a section the notebook itself calls "Appendix R" — **that last
name is misleading for reproduction purposes, see the finding below.**

**Important finding (verified against the actual code, not just the thesis text):** the notebook's
own docs frame "Appendix R" as a disposable check — reproducing an old, "methodologically
superseded" evaluation protocol against the corrected Parts II–IV pipeline elsewhere in the same
notebook. That framing is backwards for us: the thesis's own text (§8.9, "THE WINNING RESULT")
describes exactly this computation — n-gram/HMM/hybrid over all 244 tokens starting from position
1, not the "common-target" 217-target restriction Parts II–IV use — as what actually produced
**Table 8.7, the thesis's headline Task 3 result.** Confirmed by matching numbers: "Appendix R"'s
own `REPORT_REFERENCE` dict (`ngram_backoff_h2: (0.604, 0.499)`, etc.) is *exactly* Table 8.7's
published values. There's also a genuine reproducibility trap baked into the source code: it only
hits those exact numbers when the token `group` column is treated as **text**, not int — the
notebook's own comment records having verified both (`int order -> h=2 0.596/0.460 ; text order ->
0.604/0.499`) and chose text deliberately for tie-breaking reasons. Ported faithfully, quirk and
all, in `src/models/task3_grammar.py`'s docstring and `_prepare_text_group_tokens()`.

| Notebook part | Produces | Repo destination | Status |
|---|---|---|---|
| Part I (cell 8, = `master_feature_generator_..._V4.ipynb` code_cells[18]) — build six-label activity tokens | input to everything else in Ch.8 | `src/models/task3_tokens.py` | ✅ **ALL 9 GROUPS real-data, per-row, per-column PASS — freshly re-confirmed 2026-09-12 by actually re-running all 9 groups' validation scripts (not trusting the 2026-09-06 milestone log entry blindly).** This row previously understated status: a 2026-09-06 session entry ("MILESTONE: Task 3 six-label activity tokens — exact PASS, ALL 9 groups") had already scaled Group 1's 2026-09-05 pilot to all 9 groups and generated `run_group{1,2,3,5,6,7,8,9,10}_task3_tokens_validation.py` + empty (0-mismatch) report CSVs for every group, but this doc's own row was never updated to reflect it. Re-ran all 9 scripts for real this session: from-scratch build (RQ3 normalized labels + already-validated `interaction_eng3_features.csv`, via `load_six_label_windows`/`build_fullstat_tokens`) matches the official `activity_tokens_6label_fullstat.csv` **exactly for every group** — per-group row counts {1:15, 2:31, 3:39, 5:23, 6:12, 7:33, 8:40, 9:40, 10:11} (sum 244, matching the official total exactly), 340/340 columns, 0 mismatches at 1e-6 tolerance, every group. See `data/external/thesis_data/RAW_VALIDATION_FEATURES/run_group{1,2,3,5,6,7,8,9,10}_task3_tokens_validation.py` and their `group{N}_task3_tokens_validation_report.csv` outputs (all empty). This still does not by itself prove the same code path produced the *original* historical Table 8.x numbers (see module docstring) — only that the ported mechanism is correct and reproducible from its stated upstream inputs. **2026-09-12 update: one of those two upstream inputs (RQ3 normalized labels) is no longer a bare pre-existing dependency either** — see the "Label cleaning / annotation normalization" row above, now closed end-to-end from raw sensor data with a tiny, precisely-characterized 2/2080-row residual on non-classification-affecting columns. The other input, `interaction_eng3_features.csv`, was already proven bit-exact from raw `model_ready` data for all 9 groups in earlier sessions (Table 7.9's ENG3 validation). |
| "Appendix R" (cells 50–54) — n-gram/HMM/hybrid grammar over all 244 tokens | **Table 8.7 — THE WINNING RESULT** | `src/models/task3_grammar.py` | 🟨 ported + smoke-tested on synthetic data; not yet run against the real token table |
| Part II (cell 10) — corrected common-target grammar/sensor/hybrid (217 targets) | `segment_markov_h1` feeds **both** Table 8.4's "Segment Markov" row AND Table 8.7's "First-order segment Markov" row. The other 4 models' common-target numbers aren't directly tabulated — narrative-only, motivating the task3_grammar.py follow-up (§8.9 "the earlier comparison") | `src/models/task3_common_targets.py` | ✅ **real-data run done (2026-09-06), verified faithful to source — thesis-text reference appears stale, not a code bug.** `segment_markov_h1` (n=217) computed: pooled_accuracy=0.4793, pooled_macro_f1=0.2875. `docs/thesis_reproduction_targets.md` states 0.481/0.285 for this row, but the **source notebook's own saved execution output** (`notebooks_reference/task3_FINAL_V2_with_exact_report_reproduction.ipynb`, the cell that produces this row) shows **0.4793/0.2875 — an exact 4-decimal match to this port's real-data output**. The figure "0.481"/"0.285" appears nowhere in the notebook's own code or stored cell outputs, only in the thesis-text transcription doc. Conclusion: the port is provably faithful to the notebook it was ported from; the discrepancy against the transcribed thesis-text figure is most likely a rounding/transcription artifact in that doc (or a different historical execution run than the one archived in the notebook) — not a code bug, no fix applied. This is the only one of Table 8.4's 4 rows computable without neural training; the other 3 (Transformer labels-only, Transformer+sensors, LSTM+sensors) need `task3_neural.py` / Part III's real training, deliberately out of scope for this pass (CPU-slow). Other primary-panel models (n=217, not tabulated in any published table, for the record): `hybrid_ngram3_sensor` 0.581/0.501, `ngram_backoff_h3` 0.585/0.495, `second_order_categorical_markov` 0.581/0.424, `ngram_backoff_h2` 0.581/0.424, `sensor_next_logreg` 0.244/0.233. Long-history variant (n=154, positions 10+, no direct published row) also ran successfully, best row `ngram_backoff_h3` 0.604/0.503. |
| Part III (cell 12) — leakage-free neural (Transformer/LSTM) on 217 common targets, 3 seeds | Table 8.4 (Transformer labels-only / Transformer+sensors / LSTM+sensors rows) | `src/models/task3_neural.py` | ⚠️ **first real-data run completed (2026-09-06/07), all 12 fold runs (2 model kinds × 2 feature settings × 3 seeds) finished on real data — close but not exact, same-direction gap on every row.** Full run (CPU-only, no CUDA), real 217-common-target token table via `task3_tokens.get_or_build_tokens`. Computed mean ± population SD (ddof=0) across seeds {42,1,7} from `data/external/thesis_data/PUBLICATION_TASK3_CORRECTED_FINAL/table_8_4_neural_validation/TOKEN_NEURAL_CHECKPOINTS_FINAL_V2_COMMON_TARGETS/*summary.csv`: Transformer labels-only A=0.453±0.021/M=0.314±0.023 vs published 0.464±0.018/0.332±0.021 (diff -0.011/-0.018); **Transformer labels+sensors** (the thesis's best neural result) A=0.541±0.004/M=0.393±0.010 vs published 0.563±0.014/0.424±0.020 (diff -0.022/-0.031); LSTM labels+sensors A=0.462±0.043/M=0.301±0.038 vs published 0.482±0.012/0.340±0.021 (diff -0.020/-0.039, and this port's own seed-to-seed SD is ~3-4x wider than published — one seed (1) landed notably lower than the other two). **Qualitative finding reproduces correctly** (Transformer+sensors best, labels-only variants weaker, LSTM+sensors in between) — same ranking as the thesis's own §8.9 point 3. Every row undershoots by a consistent 0.01-0.04, in the same direction each time — consistent with ordinary neural-training stochasticity beyond the 3 named seeds (dropout/init/BLAS nondeterminism not fully pinned by `torch.manual_seed` alone), the same class of effect already documented for Table 8.5's Transformer rows, rather than a code bug. Not chased further — no fix applied, no bug found with confidence. This closes out Table 8.4: all 4 of its rows now have real-data numbers (Segment Markov row from Part II, close match; these 3 neural rows, close-but-not-exact) — Chapter 8's `task3_*` module set has no remaining never-run modules. |
| Part IV (cell 15) — merge II+III into publication tables | assembles the final Table 8.4-shaped panel (Segment Markov from Part II + 3 neural rows from Part III) | `src/models/task3_publication.py` | ✅ ported + smoke-tested — **Task 3 core pipeline (Parts I–IV) complete** |
| Appendix A (cells 18–27) — window-level 7-label history, all-window vs. transition-only | Table 8.2 (§8.4 "Persistence Problem") | `src/models/task3_persistence.py` | ✅ **real bug found and fixed (2026-09-06), 6 of 9 rows now EXACT, remaining 3 within known sklearn-version drift.** Supersedes an earlier same-day diagnosis (below) that concluded the published reference itself was stale — that was wrong; the actual cause was a code bug in this port. Root cause: cells 17-18 of `task3_FINAL_V2_with_exact_report_reproduction.ipynb` literally apply the same social/task-conversation MERGE6 collapse used everywhere else in Task 3, and the port faithfully copied that — but Table 8.2's own title ("Window-level **7-label** next-window prediction") is literally accurate, unlike Table 8.3's title which explicitly says "6-label." Confirmed by direct A/B test against `RQ3_LABEL_NORMALIZATION/rq3_normalized_labels_full.csv`: with MERGE6 applied, all 9 `REPORT_REFERENCE` rows differ (e.g. `repeat_current_label`/h1/all_windows gives 0.8865/0.7578 vs. published 0.8672/0.7425, and transition counts run ~40 short: 235 vs. published 275 at h=1, 231 vs. 270 at h=5); with MERGE6 *not* applied (true 7-class `rq3_process_label` vocabulary), `n` matches published exactly for every one of the 9 rows and 6 of 9 are bit-exact (`repeat_current_label`, `logreg_label_history_only`, `ngram_markov_backoff_h1` all at h=1/all_windows; `ngram_markov_no_self_backoff_h5`/transition_only; `repeat_current_label`/transition_only). The remaining 3 (all sensor-feature-involving logistic-regression models: `logreg_sensor_history_only` h5/all_windows and h1/transition_only, `logreg_label_plus_sensor_history` h5/transition_only) are off by only 0.0006–0.0041 — consistent with the "unpinned sklearn version" numerical drift already documented elsewhere in this reproduction effort (Table 7.8/7.9's XSENS-involving conditions), not a new discrepancy. This means the earlier same-day diagnosis's "244 tokens / 235 transitions is the real, current-vintage number, so the published 275 must be stale" reasoning had it backwards: 235 is the *merged* 6-label transition count (matches `task3_tokens.py`'s 244-token / 6-class corpus, which is a *different* computation — token-level segments, not window-level next-window prediction), while 275 is simply the un-merged 7-label window-to-window transition count from the *same currently-installed* label file — no vintage mismatch, no stale reference, just the wrong label column being fed to Appendix A's evaluation. Fixed by adding an `apply_merge6` parameter (default `True`) to the shared `load_normalized_labels()`/`select_and_merge_feature_file()` functions — default preserves task3_segment_forecast.py's already-exact Table 8.3 behavior (which genuinely needs the merge — see Appendix B row below and its own module docstring), while `task3_persistence.py`'s own `run_all()` explicitly passes `apply_merge6=False`. See `task3_persistence.py`'s module docstring "REPRODUCIBILITY NOTE" and `data/external/thesis_data/PUBLICATION_TASK3_CORRECTED_FINAL/run_table_8_2_persistence_validation.py`. Naming quirk (corrected from earlier docstring text): despite looking like a stray "7-LABEL" name applied to a not-actually-7-class computation, Table 8.2 genuinely *is* 7-class — the "6-class same as everywhere else in Task 3" claim in an earlier version of this row and the module docstring was itself the bug's origin. |
| Appendix B (cells 29–41) — segment-level sensor-feature forecast + decode | Table 8.3 — **now disambiguated**: entirely this module's own output, not Part II's. Its `label_markov_segment_baseline` (0.5106/0.2919/0.3775, n=235) exactly matches the thesis's "Label Markov segment baseline" row; Part II's `segment_markov_h1` (0.481/0.285) is a different, unrelated computation (common-target-restricted) that merely sounds similar | `src/models/task3_segment_forecast.py` | ✅ **real-data PASS, 5 of 5 referenced rows EXACT (2026-09-06)**, zero code changes needed. Ran against the same `RQ3_LABEL_NORMALIZATION/rq3_normalized_labels_full.csv` + `INTERACTION_ENG3/interaction_eng3_features.csv` inputs as Appendix A, with the module's existing (unmodified) 6-label MERGE6 collapse left in place — correct here since Table 8.3's own title literally says "6-label" (unlike Table 8.2's genuinely-7-label Appendix A, see row above): `label_majority_segment_baseline` 0.3702/0.0901, `label_markov_segment_baseline` 0.5106/0.2919, `forecast_next_features_then_decode_ridge` 0.3021/0.2742, `forecast_next_features_then_decode_random_forest` 0.3230/0.1679, `oracle_decode_true_next_features_ridge` 0.3186/0.2753 — all n and both metrics bit-exact against `docs/thesis_reproduction_targets.md` Table 8.3. See `data/external/thesis_data/PUBLICATION_TASK3_CORRECTED_FINAL/run_table_8_3_segment_forecast_validation.py`. |
| Appendix C (cells 43–44) — expanding-prefix fine-13 vs. coarse-4 next-segment | Table 8.5 | `src/models/task3_expanding_prefix.py` | ✅ **real-data run done (2026-09-06), 5 of 7 referenced rows EXACT.** Reads its own input pair (`INTERACTION_ENG3/recognition_interaction_window_label_inventory.csv` + `interaction_eng3_features.csv`), not the RQ3_LABEL_NORMALIZATION output Appendix A/B use — but the inventory file itself was missing from the local data mirror (and its raw upstream input, `ALL_MODEL_READY_FILES_IDENTITY_FIXED`, is also absent, so it couldn't be regenerated from scratch either). Recovered rather than fabricated: `RQ3_LABEL_NORMALIZATION/rq3_normalized_labels_full.csv` turns out to carry that exact missing file's original 11 columns through unmodified (its own generating notebook, `rq3_label_audit_and_normalization_CODE_ONLY.py` lines 229-230/256-257, reads `recognition_interaction_window_label_inventory.csv` as its `MAIN_FILE`; column-for-column match confirmed against `master_feature_generator_..._V4_CODE_ONLY.py`'s CELL 12 schema, lines 2735-2747; a merge dry-run against the local `interaction_eng3_features.csv` matched all 2080 rows on (group, window_start, window_end) with zero misses) — those 11 columns were extracted and written once to the expected path so the module runs completely unmodified. No module constants needed restoring: `MIN_WINDOWS`/`MAX_EPOCHS`(80)/`BATCH_SIZE`/`LR`/`WEIGHT_DECAY`/`EMB_DIM`/`HIDDEN_DIM`/`MAX_SUFFIX_ORDER` already matched the real notebook's cells 43-44 settings block verbatim (lines 3571-3596) — the "reduced epoch count for speed" mentioned previously was only an ad hoc smoke-test call-site override (`max_epochs=` passed to `run_all()`), never a change to the module's own constants. Real-data result: all 4 `coarse_4` reference rows (`cnn1d` 0.657/0.564, `suffix_backoff` 0.708/0.561, `transformer` 0.653/0.557, `markov_last` 0.722/0.409, all n=216) and the `fine_13`/`suffix_backoff` row (0.355/0.169, n=287) reproduce **EXACT**; 2 of 7 DIFFER, both `transformer` rows on `fine_13`: `activity_only` acc 0.300 vs published 0.254 (+0.046), macro-F1 0.219 vs 0.178 (+0.041); `activity_plus_segment_features` acc 0.352 vs 0.345 (+0.007), macro-F1 0.163 vs 0.173 (-0.010) — consistent with the module's own documented stochastic-training caveat (fresh model+optimizer per LOGO fold, no bit-exact cross-run guarantee even with a fixed seed); notably `coarse_4`'s `transformer` row reproduced exactly while `fine_13`'s did not, suggesting the variance is amplified by fine_13's 13-class/smaller-per-fold setting rather than a code bug — not chased further. See `data/external/thesis_data/PUBLICATION_TASK3_CORRECTED_FINAL/run_table_8_5_expanding_prefix_validation.py` and its `table_8_5_expanding_prefix_validation/` output dir. |
| Appendix D (cells 46–48) — HMM / 5-class collective-state, causal vs. Viterbi | Table 8.6 | `src/models/task3_hmm_appendix_d.py` | ✅ **real-data run done (2026-09-06), 7/8 published rows exact, 1 close — all 8 rows now checked.** Ground truth: `docs/thesis_reproduction_targets.md` §8.8 Table 8.6 (8 rows, including the n=259 transition-only 5-class pair — initially blocked by a missing input file, then unblocked: `INTERACTION_OPTI2/interaction_opti2_10s.csv` fetched directly from Drive, 8.999MB, under the 10MB direct-API threshold, no Chrome needed). Full result: (1) 3-class all-windows `repeat_last` 0.955/0.955 vs published 0.955/0.955 — **EXACT**. (2) 3-class all-windows `Markov` 0.955/0.955 vs 0.955/0.955 — **EXACT**. (3) 3-class transition-only `Markov_transition` 0.886/0.627 vs 0.886/0.627 — **EXACT**. (4) 3-class transition-only Gaussian `HMM` 0.304/0.301 vs 0.304/0.301 — **EXACT**. (5) 5-class all-windows `HMM_causal_prediction` 0.585/0.546 vs 0.585/0.546 — **EXACT**. (6) 5-class all-windows `HMM_viterbi_smoothing` 0.646/0.601 vs published 0.643/0.599 — **close but not exact** (+0.003 acc / +0.002 macro_f1); notably the one row that differs despite sharing identical LOGO folds/timeline/LogReg with the exactly-matching causal-prediction row from the same cell — likely a minor decode/tie-break nuance, not chased further. (7) 5-class transition-only `HMM_categorical` (n=259) 0.282/0.265 vs 0.282/0.265 — **EXACT**. (8) 5-class transition-only `HMM_sensor` (n=259) 0.282/0.202 vs 0.282/0.202 — **EXACT**. **7 of 8 rows bit-exact, 1 row within ~0.3 percentage points.** No code bugs found; `hmmlearn` (0.3.3) already installed. Extra rows produced with no published-table counterpart (diagnostic only, not mismatches): `Markov_transition`=0.039/0.029 and `HMM`=0.496/0.492 in the all-windows 3-class file, `LogReg_before`=0.583/0.580 in the 5-class all-windows file. Table's rows are assembled from 3 separately self-contained notebook cells (kept as 3 functions, not force-merged) — cells 47/48 read the raw `ALL_MODEL_READY_FILES_IDENTITY_FIXED` per-group CSVs directly rather than the RQ3_LABEL_NORMALIZATION output; the validation driver monkeypatches the file-finder to point at `RAW_VALIDATION_FEATURES/group_{g}/...` instead (module itself untouched) |
| naive-5 rerun (grammar + persistence) | Table 8.8 | `src/models/task3_naive5.py` | ⚠️ **two real bugs found and fixed (2026-09-06), 6 of 7 referenced rows now EXACT; 1 row remains a genuine, unexplained miss.** `NAIVE_GROUPS={2,3,5,6,10}`. Background: the persistence half must call `task3_persistence.load_normalized_labels()`/`select_and_merge_feature_file()` with **`apply_merge6=True`** (the opposite of Table 8.2's own `run_all()`, which needs `False`) — confirmed against the thesis's own prose "111 transitions in 942... examples" (reproduces bit-exact only under the 6-class merge; the true 7-class vocabulary gives 118). So the *full-cohort* Table 8.2 pipeline needs the 7-class vocabulary but *this same pipeline re-run on the naive-5 subset* for Table 8.8 needs the 6-class one — a genuine inconsistency in the original thesis computation between the two tables, not a bug in this port (see `task3_naive5.py`'s `run_naive5_persistence()` docstring/comment for the full A/B evidence trail). **Bug 1 (grammar rows, fixed)**: `ngram_backoff_h1`/h2/h3/h5 — G2/G3/G5/G10 always matched published to ~3 decimals, but G6 was consistently off by ~0.012-0.013 at every history length (e.g. h1: computed 0.133 vs published 0.121). Root-caused precisely: `task3_grammar.run_ngram_backoff()` unconditionally casts `group` to **text** before sorting training groups for the back-off `Counter` tables (documented, verified-correct behavior for Table 8.7's full 9-group cohort — see that module's docstring). For the naive-5 SUBSET `{2,3,5,6,10}`, text-sorting the *training* groups differs from int-sorting **only** when G6 is the held-out fold (training set `{2,3,5,10}` sorts as `'10','2','3','5'` as text vs. `2,3,5,10` as int) — this flips `Counter.most_common()` tie-breaks for G6's predictions specifically, and only G6's. Fixed by adding a `group_as_text` parameter to `run_ngram_backoff()` (default `True`, preserving Table 8.7's exact behavior); `task3_naive5.py`'s `run_naive5_grammar()` now passes `group_as_text=False`. Verified: G6 now reproduces published exactly at all 4 history lengths (h1 0.121, h2 0.366, h3 0.188, h5 0.205), and G2/G3/G5/G10 are numerically identical to 4 decimals under either setting (zero regression risk). **Bug 2 (`repeat_current_all_windows`, fixed)**: G3/G5/G6 always matched published exactly (0.661/0.774/0.582); G2 (0.407 vs 0.488) and G10 (0.632 vs 0.759) were substantially off. Root cause: `task3_naive5.py`'s `_per_group_macro_f1()` scored every held-out naive group's macro-F1 against one shared `all_labels` list pooled across all 5 naive groups' windows, instead of each group's own locally-occurring label set (the union of that group's `y_true` and its predictions — sklearn's own `f1_score(..., average="macro")` default when `labels=` isn't passed). G2 never has a `merging` window and G10 never has an `inspection` window in the naive-5 subset, so scoring them against the pooled 6-class list silently counted a zero-score term for a class they don't have, dragging their macro-F1 down — G3/G5/G6 happen to contain every naive-5 class already, so they were unaffected either way. Confirmed empirically: G2's local-label macro-F1 is 0.4877 (pooled-label was 0.4065) and G10's is 0.7588 (pooled-label was 0.6324) — both exact matches to the published 0.488/0.759. Fixed by computing `local_labels` per held-out group inside `_per_group_macro_f1()` instead of reusing the module-level `all_labels`. **Re-run end-to-end after both fixes** (`data/external/thesis_data/PUBLICATION_TASK3_CORRECTED_FINAL/run_table_8_8_naive5_validation.py`, real data): `ngram_backoff_h1/h2/h3/h5` and `repeat_current_all_windows` and `repeat_current_transitions` are now all **EXACT** (6 of 7 referenced rows) — see `data/external/thesis_data/PUBLICATION_TASK3_CORRECTED_FINAL/table_8_8_naive5_validation/task3_naive5_table_8_8.csv`. **Remaining, still-open, honestly-reported gap**: `no_self_ngram_transitions` — the thesis's own text notes this row is numerically identical to `ngram_backoff_h1` in the published table (an intentional coincidence at h=1); this port's real-data computation still does NOT reproduce that coincidence after both fixes above (G2 0.297, G3 0.220, G5 0.127, G6 0.188, G10 0.074 vs. published 0.190/0.278/0.234/0.121/0.074 — only G10 matches; neither the group_as_text nor the local-label-set fix changes this row, both were tested directly against it). This is a different computation (`task3_persistence.ngram_markov_no_self_backoff_h1` on window-level history, not `task3_grammar.ngram_backoff_h1` on token history) and remains genuinely unexplained — flagged for a future session rather than guessed at. Deliberately does **not** force-match the thesis's flagged "h=1 back-off = no-self-transition" coincidence — computes both independently, and the real-data run confirms they are NOT the same computation in this port |
| Cell 56 (large model-ready data inspection) | nothing — leftover exploration tooling | intentionally not porting | — |

## Known duplication to resolve before porting (see `drive_source_inventory.md` §4 for full detail)

- `github notebooks/` root has 5 generations of `01_interaction_vs_noninteraction*` and 4 of
  `master_feature_generator*` — **use `actual ones/` subfolder + `_CORRECTED_V4`, ignore the rest.**
- `CLEAN_ML/` (ironically) has the worst duplication: `01_interaction_detection.ipynb` ×3,
  `02_activity_recognition.ipynb` ×2 (byte-identical), `03_activity_prediction.ipynb` ×2
  (byte-identical). Diff before picking one.
- `PRE_PROCESSING/` has 4 overlapping `sensor_sync*` variants and 3 generations of
  `Arda_Thesis_Data_Preprocessing*` — start from `_FIXED_v4`.
- `thesis/output/group_3` contains files misnamed `group5_oe_labelled...csv` — **verify by hand**
  whether this is genuinely group 3's data before trusting it as a source.

## Open questions to resolve while porting (see `thesis_reproduction_targets.md` §8.2/§8.3)

- `OE10` feature set is named in the thesis prose but never independently tabulated — check
  whether the actual notebook code defines it even though no results table reports it alone.
- Table 4.2's duplicate `video_time_s` row (conflicting descriptions) needs resolving against
  actual notebook code, not just the PDF text.
- Table 7.8's GRU row uses a `seq=3, 30s` windowing convention that doesn't match the `seq=9/18`
  pattern used everywhere else — confirm in the OE-conversation notebook whether this is real or
  a transcription artifact.
- Session-duration figures differ between Table 3.1 (raw recording) and Table 10.1 (ELAN
  timeline) — pick one convention per pipeline stage and document which.
